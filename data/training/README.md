Current training data lives in `cleaning_retention_wf05/`.

The classifier uses:

- `face_particle_retention.csv`
- `dirty_surface_neighbor_context.csv`
- `form_particle_retention.csv`

Generated experiments and model outputs belong in `cache/`, not here.
